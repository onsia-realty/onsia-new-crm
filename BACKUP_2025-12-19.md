# 온시아 CRM 백업 및 작업 기록

**작업 일자**: 2025년 12월 19일
**작성자**: Claude Code
**프로젝트**: 온시아 부동산 CRM 시스템

---

## 📋 작업 내용 요약

### 1. 부재 고객 관리 기능 구현 및 개선

#### 1-1. 고객 상세페이지 부재 기록 기능 (커밋: c7e6048)
- **위치**: `app/dashboard/customers/[id]/page.tsx`
- **기능**: 통화 기록 카드 헤더에 [부재] 버튼 추가
- **동작**:
  - 클릭 시 자동으로 "1차 부재", "2차 부재" 등 순차적으로 기록
  - 기존 부재 기록 개수를 카운트하여 다음 번호 자동 생성
  - PhoneOff 아이콘 및 주황색 테마 적용

```typescript
// 부재 기록 추가 함수
const handleAddAbsence = async () => {
  if (addingAbsence) return;

  setAddingAbsence(true);
  try {
    // 기존 부재 기록 개수 확인
    const absenceCount = callLogs.filter(log =>
      log.content.includes('부재')
    ).length;

    const absenceNumber = absenceCount + 1;
    const absenceContent = `${absenceNumber}차 부재`;

    // API 호출하여 부재 기록 저장
    // ...
  } catch (error) {
    // 에러 처리
  } finally {
    setAddingAbsence(false);
  }
};
```

#### 1-2. 부재 고객 필터 기능 (커밋: ace7352, 4982276)
- **위치**: `app/dashboard/customers/page.tsx`, `app/api/customers/route.ts`
- **기능**: 통계 카드에서 부재 고객만 필터링하여 표시
- **개선사항**:
  - 부재 고객 카드를 2번째 위치로 이동 (전체 고객 바로 옆)
  - 모바일에서도 표시되도록 반응형 적용
  - 마지막 통화가 부재인 고객만 정확하게 필터링 (Raw SQL 쿼리 사용)

```sql
-- 마지막 통화가 부재인 고객 조회 쿼리
SELECT c.id
FROM "Customer" c
INNER JOIN (
  SELECT "customerId", MAX("createdAt") as "lastCallAt"
  FROM "CallLog"
  GROUP BY "customerId"
) latest ON c.id = latest."customerId"
INNER JOIN "CallLog" cl ON cl."customerId" = c.id AND cl."createdAt" = latest."lastCallAt"
WHERE c."isDeleted" = false
AND cl.content LIKE '%부재%'
ORDER BY c."displayOrder" ASC NULLS LAST, c."createdAt" DESC
```

### 2. 고객 정렬 순서 개선
- **displayOrder** (엑셀 순서) → **assignedAt/createdAt** 순으로 정렬
- NULL 값 처리 (NULLS LAST)
- 직원별 조회 시와 전체 조회 시 일관된 정렬 적용

### 3. 중복 고객 필터링 로직 추가
- 중복된 전화번호를 가진 고객만 필터링하는 기능 구현
- `showDuplicatesOnly` 파라미터 추가

### 4. 통계 API 개선
- **위치**: `app/api/statistics/route.ts`
- 부재 고객 계산 로직 정확도 개선
- 직원별/전체 통계 분리 처리

---

## 📝 커밋 내역

### 최근 5개 커밋
```
da36b7b - chore: Service Worker 업데이트 및 .gitignore 정리
4982276 - fix: 부재 고객 필터 개선 및 정렬 순서 수정
c544f1c - style: 부재 고객 카드를 전체 고객 바로 옆으로 이동
ace7352 - feat: 부재 고객 필터 기능 추가
c7e6048 - feat: 고객 상세 페이지에 부재 기록 기능 추가
```

### 주요 변경 파일
- `app/dashboard/customers/[id]/page.tsx` - 부재 기록 버튼 추가
- `app/dashboard/customers/page.tsx` - 부재 필터 UI 개선
- `app/api/customers/route.ts` - 부재 고객 필터링 로직
- `app/api/statistics/route.ts` - 통계 계산 로직 개선
- `app/api/customers/bulk-import/route.ts` - 일괄 등록 개선
- `components/dashboard/EmployeeDashboard.tsx` - 대시보드 통계 표시
- `.gitignore` - Claude Code 설정 파일 제외

---

## 🚀 배포 정보

### GitHub 저장소
- **Repository**: `https://github.com/onsia-realty/onsia-new-crm.git`
- **Branch**: `main`
- **최신 커밋**: `da36b7b`

### Vercel 프로덕션 배포
- **URL**: `https://onsia-new-crm-main-2em53qnht-realtors77-7871s-projects.vercel.app`
- **배포 일시**: 2025년 12월 19일
- **빌드 상태**: ✅ 성공
- **빌드 시간**: 약 1분

---

## 💻 다른 PC에서 프로젝트 사용하기

### 방법 1: 처음 설정하는 경우

```bash
# 1. Git clone
git clone https://github.com/onsia-realty/onsia-new-crm.git
cd onsia-new-crm

# 2. pnpm 설치 (없는 경우)
npm install -g pnpm

# 3. 의존성 설치
pnpm install

# 4. 환경 변수 설정
# .env 파일 생성 후 아래 내용 입력
cat > .env << 'EOF'
# Database
DATABASE_URL="postgresql://..."

# NextAuth
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="..." # openssl rand -base64 32 로 생성

# Email (선택)
EMAIL_SERVER="smtp://..."
EMAIL_FROM="noreply@onsia.local"
EOF

# 5. Prisma 설정
pnpm prisma generate
pnpm prisma db push

# 6. 시드 데이터 (선택)
pnpm prisma db seed

# 7. 개발 서버 실행
pnpm dev
```

### 방법 2: 이미 설정된 PC에서 최신 코드 가져오기

```bash
# 1. 최신 코드 가져오기
git pull origin main

# 2. 의존성 업데이트 (package.json 변경 시)
pnpm install

# 3. Prisma 재생성 (schema 변경 시)
pnpm prisma generate
pnpm prisma db push

# 4. 개발 서버 재시작
pnpm dev
```

---

## 🗂️ 프로젝트 구조

```
onsia-new-crm/
├── app/
│   ├── api/
│   │   ├── customers/          # 고객 API
│   │   │   ├── route.ts        # 고객 CRUD + 필터링
│   │   │   ├── bulk-import/    # 엑셀 일괄 등록
│   │   │   └── [id]/           # 고객 상세
│   │   ├── call-logs/          # 통화 기록 API
│   │   ├── statistics/         # 통계 API
│   │   └── ...
│   ├── dashboard/
│   │   ├── customers/          # 고객 관리 페이지
│   │   │   ├── page.tsx        # 고객 목록 (부재 필터 포함)
│   │   │   ├── [id]/page.tsx   # 고객 상세 (부재 버튼 포함)
│   │   │   └── new/            # 고객 등록
│   │   └── ...
│   └── ...
├── components/
│   └── dashboard/
│       └── EmployeeDashboard.tsx  # 직원 대시보드
├── lib/
│   ├── auth.ts                 # NextAuth 설정
│   ├── prisma.ts               # Prisma 클라이언트
│   └── ...
├── prisma/
│   ├── schema.prisma           # 데이터베이스 스키마
│   └── seed.ts                 # 시드 데이터
├── .env                        # 환경 변수 (git 제외)
├── .gitignore
├── package.json
└── README.md
```

---

## 🔑 주요 기능 목록

### ✅ 구현 완료 기능

1. **인증 및 권한 관리**
   - NextAuth v5 기반 로그인/회원가입
   - 역할 기반 접근 제어 (EMPLOYEE, LEADER, HEAD, ADMIN)
   - 관리자 승인 시스템

2. **고객 관리**
   - 고객 CRUD (등록, 조회, 수정, 삭제)
   - 엑셀 일괄 등록 (displayOrder 순서 유지)
   - 전화번호 중복 탐지 및 병합
   - 중복 고객 필터
   - **부재 고객 관리** ⭐ NEW
     - 상세페이지에서 [부재] 버튼으로 1차, 2차, 3차... 자동 기록
     - 통계 카드에서 부재 고객만 필터링

3. **통화 기록**
   - 통화 내용 등록/수정/삭제
   - 부재 기록 자동 생성
   - 타임라인 형태로 표시

4. **방문 일정 관리**
   - 캘린더 기반 일정 등록
   - 상태 관리 (예정/완료/취소/노쇼)
   - 카카오톡 공유 기능

5. **블랙리스트 관리**
   - 블랙리스트 등록/해제
   - 전화 금지 경고 배너 표시

6. **통계 및 대시보드**
   - 전체 고객 수
   - 부재 고객 수
   - 오늘 통화 건수
   - 예정 방문 건수
   - 중복 고객 수

7. **담당자 변경**
   - 담당자 변경 요청
   - 변경 이력 추적

8. **계약 관리**
   - 계약 대장 연동

9. **광고콜 관리**
   - 광고콜 등록 및 배분

10. **공지사항**
    - 공지 CRUD
    - 상단 고정 기능

---

## 🛠️ 기술 스택

### Frontend
- **Framework**: Next.js 14 (App Router)
- **Language**: TypeScript
- **UI**: Tailwind CSS + shadcn/ui
- **State**: React Query (TanStack Query)
- **Form**: React Hook Form + Zod

### Backend
- **Runtime**: Next.js API Routes
- **Database**: PostgreSQL (Vercel Postgres)
- **ORM**: Prisma
- **Auth**: NextAuth v5

### DevOps
- **Hosting**: Vercel
- **Version Control**: GitHub
- **Package Manager**: pnpm
- **CI/CD**: Vercel 자동 배포

---

## 📊 데이터베이스 스키마

### 주요 테이블

#### Customer (고객)
```prisma
model Customer {
  id              String    @id @default(cuid())
  name            String
  phone           String    @unique
  assignedUserId  String?
  assignedAt      DateTime?
  displayOrder    Int?      // 엑셀 순서
  isDeleted       Boolean   @default(false)

  // 관계
  callLogs        CallLog[]
  visitSchedules  VisitSchedule[]

  createdAt       DateTime  @default(now())
  updatedAt       DateTime  @updatedAt
}
```

#### CallLog (통화 기록)
```prisma
model CallLog {
  id          String   @id @default(cuid())
  customerId  String
  customer    Customer @relation(fields: [customerId], references: [id])
  userId      String
  user        User     @relation(fields: [userId], references: [id])
  content     String   // "1차 부재", "2차 부재" 등 포함
  note        String?
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}
```

---

## 🐛 알려진 이슈 및 해결 방법

### 이슈 1: 부재 필터가 화면에 안 보이는 경우
**원인**: 브라우저 캐시 또는 개발 서버 미재시작
**해결**:
```bash
# 개발 서버 재시작
pnpm dev

# 또는 하드 리프레시 (브라우저)
Ctrl + Shift + R (Windows/Linux)
Cmd + Shift + R (Mac)
```

### 이슈 2: Prisma Client 오류
**원인**: schema.prisma 변경 후 generate 미실행
**해결**:
```bash
pnpm prisma generate
pnpm prisma db push
```

### 이슈 3: 환경 변수 오류
**원인**: .env 파일 누락 또는 잘못된 설정
**해결**:
```bash
# .env.example을 참고하여 .env 생성
cp .env.example .env

# 필수 환경 변수 확인
# - DATABASE_URL
# - NEXTAUTH_SECRET
# - NEXTAUTH_URL
```

---

## 📞 문의 및 지원

### GitHub 이슈
문제가 발생하면 GitHub Issues에 등록해주세요.

### 긴급 문의
프로덕션 환경에서 문제 발생 시 즉시 Vercel 로그 확인:
```bash
vercel logs [deployment-url]
```

---

## 📅 변경 이력

### 2025-12-19
- ✅ 부재 고객 관리 기능 구현
  - 고객 상세페이지 [부재] 버튼 추가
  - 부재 고객 필터 UI 개선 (2번째 위치로 이동)
  - 마지막 통화 기반 부재 고객 필터링 로직 개선
- ✅ 고객 정렬 순서 개선 (displayOrder → assignedAt/createdAt)
- ✅ 중복 고객 필터링 로직 추가
- ✅ Service Worker 업데이트
- ✅ .gitignore 정리 (Claude Code 설정 파일 제외)
- ✅ Vercel 프로덕션 배포 완료

### 2025-12-16
- ✅ 고객 상세 페이지 모바일 UI 최적화
- ✅ 광고콜 관리 기능 개선
- ✅ 고객 일괄등록 시 엑셀 순서대로 정렬 기능 추가

---

## 🔐 보안 주의사항

### 환경 변수 (.env)
**절대 Git에 커밋하지 마세요!**
- DATABASE_URL
- NEXTAUTH_SECRET
- EMAIL_SERVER

### 백업 방법
```bash
# 데이터베이스 백업 (Vercel Postgres)
# Vercel 대시보드에서 Export 기능 사용

# 코드 백업
git push origin main  # GitHub에 자동 백업됨
```

---

## 🎯 다음 작업 계획

### 우선순위 높음
- [ ] 부재 고객 알림 기능 (3일 이상 부재 시 알림)
- [ ] 통화 기록 검색 기능
- [ ] 고객 등급 자동 조정 (통화 빈도 기반)

### 우선순위 중간
- [ ] 대시보드 차트 개선
- [ ] 모바일 반응형 UI 추가 개선
- [ ] 엑셀 내보내기 기능

### 우선순위 낮음
- [ ] 다크 모드 지원
- [ ] 알림 설정 커스터마이징

---

**작성 완료**: 2025년 12월 19일
**다음 백업 예정**: 주요 기능 추가 시

---

© 2025 온시아 부동산. All rights reserved.
